// Package tmpl parses embedded HTML templates and registers template.FuncMap helpers.
// It sits between embedded assets and HTTP handlers so the HTTP layer stays focused on request/response.
package tmpl
