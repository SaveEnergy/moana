// Package money formats and parses EUR amounts in cents for templates and forms (no I/O).
// Formatting in format.go; parsing from strings in parse.go; [AbsCents] for magnitude; [AddCents] for overflow-checked sums.
package money
